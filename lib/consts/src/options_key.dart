enum FileFormatOptions {
  option1,
  option2,
  option3,
  option4,
  option5,
  option6,
  option10
}

///http[http],https[https],失败[failure]，等待[waiting]，关闭[closeHttps]
enum NavigationDrawerTypde { http, failure, waiting, closeHttps }
